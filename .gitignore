# macOS
.DS_Store

# Personal chart library edited via the app's folder mode — keep it out of the
# public app repo.
local tab collection/

# Python
__pycache__/
*.pyc

# Playwright MCP run artifacts (console logs, page snapshots).
.playwright-mcp/
